//literal types
type packagee = "typescript" | "react" | "node js"

let developPacckage:packagee = "node js"  

enum newT {
    pending ,
    starter,
    action
}

let fdunc:{types:packagee , model:newT} = {
    types:"typescript",
    model:newT.pending
}