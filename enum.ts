enum STEP {
  one,
  two,
  three,
}
console.log(STEP.three);

enum STATUS_CODES {
  OK = 200,
  CREATED = 201,
  ACCEPTED = 202,
}
console.log(STATUS_CODES.ACCEPTED);

enum TEST {
  VALIDATE = "error",
  NOT_FOUND = "notFOund",
  SERVER_ERROR = "error",
}

